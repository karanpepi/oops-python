#tuple is order collection of elements
#tuple is immutable can not be changed
#tuple can be created using tuple() method

tuple = (1,"karan",12.4,1)
print(tuple)

elements = [i for i in tuple]
print(elements)